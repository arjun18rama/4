/**
 * networkRoutes.js - Placeholder module within config.
 * Holds dummy mappings for network service endpoints.
 */

export const routeMap = {
  login: '/api/login',
  logout: '/api/logout',
  fetchData: '/api/data'
};
